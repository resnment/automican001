# Automican001

This repository now contains a single React app shell with:

- `src/App.jsx` (the full Automican UI requested)
- `src/main.js` (React entry point)
- `index.html` (root mount + Tailwind CDN)
